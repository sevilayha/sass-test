const header        = document.getElementById('site__header');
const stickySidebar = document.querySelector('.sidebar__sticky-icky');

if (stickySidebar) {
  const headerHeight      = header.clientHeight;
  const sidebarOffsetTop  = headerHeight + 40;

  // positionTheStuckThing(stickySidebar, sidebarOffsetLeft, sidebarOffsetTop);
  // stickTheThing(stickySidebar);
}